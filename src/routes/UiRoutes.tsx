import { Routes, Route } from "react-router-dom";

const UiRoutes = () => {
    return (
        <Routes>
            <Route path="/" element={<h1>سلام</h1>} />
        </Routes>
    );
};

export default UiRoutes;
